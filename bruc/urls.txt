'''from asyncio import run_coroutine_threadsafe
from django.urls import path
from . import views
from rest_framework import routers
from .views import GuestViewSet, TagsViewSet, UserViewSet

router = routers.DefaultRouter()
router.register('gguests', GuestViewSet)
router.register('ttags', TagsViewSet)
router.register('uusers', UserViewSet)



urlpatterns = [
    path('', views.home, name="home"),
    path('home', views.home, name="home"),
    path('guests', views.guests, name="guests"),
    path('tags', views.tags, name="tags"),
    path('privileges', views.privileges, name=""),
    path('users', views.users, name=""),
    path('import', views.iimport, name="import"),
    path('export', views.export, name="export"),
    path('logout', views.logout, name="logout"),
    path('login', views.login, name="login"), 
] '''